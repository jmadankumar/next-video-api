import { Request, Response } from 'express';

export const createChannel = (req: Request, res: Response): void => {
  res.send('Get All Channel');
};

export const updateChannel = (req: Request, res: Response): void => {
  res.send('Get All Channel');
};

export const getChannelById = (req: Request, res: Response): void => {
  res.send('Get Channel');
};

export const getAllChannel = (req: Request, res: Response): void => {
  res.send('Get All Channel');
};

export const deleteChannel = (req: Request, res: Response): void => {
  res.send('Get All Channel');
};